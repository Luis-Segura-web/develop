name: reproductor_iptv
description: "Reproductor IPTV completo con soporte Xtream Codes en Flutter"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  
  # Recursos para la aplicación
  assets:
    - assets/images/
    - assets/icons/
    - assets/logos/
